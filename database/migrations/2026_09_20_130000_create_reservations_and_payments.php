<?php
use Illuminate\Database\Migrations\Migration; use Illuminate\Database\Schema\Blueprint; use Illuminate\Support\Facades\Schema;
return new class extends Migration { public function up():void { Schema::create('reservations',function(Blueprint $t){$t->id();$t->foreignId('show_id')->constrained()->cascadeOnDelete();$t->foreignId('seat_id')->constrained()->cascadeOnDelete();$t->string('token')->index();$t->timestamp('expires_at')->index();$t->string('status')->default('held');$t->timestamps();$t->unique(['show_id','seat_id','status']);}); Schema::create('payments',function(Blueprint $t){$t->id();$t->foreignId('order_id')->constrained()->cascadeOnDelete();$t->string('gateway')->default('mock');$t->string('authority')->nullable()->unique();$t->unsignedInteger('amount');$t->string('status')->default('pending');$t->json('response')->nullable();$t->timestamps();});} public function down():void{Schema::dropIfExists('payments');Schema::dropIfExists('reservations');} };
