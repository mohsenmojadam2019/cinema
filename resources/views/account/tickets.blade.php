<!doctype html><html lang="fa" dir="rtl"><meta charset="utf-8"><link rel="stylesheet" href="/css/cinema.css"><main class="container"><h1>بلیت‌های من</h1>@forelse($tickets as $ticket)<article class="ticket"><h2>{{ $ticket->order->show->event->title }}</h2><p>{{ $ticket->order->show->venue->name }} · صندلی {{ $ticket->seat->row_label }}-{{ $ticket->seat->number }}</p><code>{{ $ticket->code }}</code><br><img src="{{ route('tickets.qr',$ticket) }}" width="130"></article>@empty<p>بلیت فعالی ندارید.</p>@endforelse</main>
