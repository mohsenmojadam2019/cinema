<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>جست‌وجوی پنل | سینماپلاس</title><link rel="stylesheet" href="/css/cinema.css"></head><body class="admin-crud"><main class="admin-main"><div class="section-heading"><div><span class="eyebrow">جست‌وجوی سراسری</span><h1>نتایج برای «{{ $term }}»</h1></div><a class="btn btn-outline" href="{{ route('admin.dashboard') }}">داشبورد</a></div><div class="dash-grid"><section class="admin-card"><h2>رویدادها</h2>@forelse($events as $event)<p><a href="{{ route('admin.events.edit',$event) }}">{{ $event->title }}</a></p>@empty<p class="muted">نتیجه‌ای نیست.</p>@endforelse</section><section class="admin-card"><h2>کاربران</h2>@forelse($users as $user)<p>{{ $user->name }} · {{ $user->phone ?: $user->email }}</p>@empty<p class="muted">نتیجه‌ای نیست.</p>@endforelse</section><section class="admin-card"><h2>سفارش‌ها</h2>@forelse($orders as $order)<p><a href="{{ route('admin.orders.index') }}">{{ $order->code }}</a> · {{ $order->user?->name ?: 'مهمان' }}</p>@empty<p class="muted">نتیجه‌ای نیست.</p>@endforelse</section></div></main></body></html>
