<!doctype html><html lang="fa" dir="rtl"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>تراکنش‌ها | سینماپلاس</title><link rel="stylesheet" href="/css/cinema.css"></head><body class="admin-crud"><main class="admin-main"><div class="section-heading"><div><span class="eyebrow">مالی و درگاه</span><h1>تراکنش‌ها</h1></div><a class="btn btn-outline" href="{{ route('admin.dashboard') }}">داشبورد</a></div><section class="admin-card"><form class="search" method="get"><input class="field" name="q" value="{{ request('q') }}" placeholder="مرجع یا کد پیگیری"><select class="field" name="status"><option value="">همه وضعیت‌ها</option>@foreach(['paid'=>'موفق','pending'=>'در انتظار','failed'=>'ناموفق','refunded'=>'بازپرداخت‌شده'] as $key=>$label)<option value="{{ $key }}" @selected(request('status')===$key)>{{ $label }}</option>@endforeach</select><button class="btn btn-red">فیلتر</button></form><div style="overflow:auto"><table><thead><tr><th>سفارش</th><th>کاربر</th><th>مبلغ</th><th>درگاه</th><th>مرجع</th><th>وضعیت</th><th>زمان</th></tr></thead><tbody>@forelse($payments as $payment)<tr><td>{{ $payment->order?->code ?: '—' }}</td><td>{{ $payment->order?->user?->name ?: 'مهمان' }}</td><td>{{ number_format($payment->amount) }} تومان</td><td>{{ $payment->gateway ?: '—' }}</td><td>{{ $payment->reference ?: $payment->authority ?: '—' }}</td><td>{{ $payment->status }}</td><td>{{ optional($payment->created_at)->format('Y/m/d H:i') }}</td></tr>@empty<tr><td colspan="7">تراکنشی پیدا نشد.</td></tr>@endforelse</tbody></table></div>{{ $payments->links() }}</section></main></body></html>
