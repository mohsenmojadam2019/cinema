<!doctype html><html lang="fa" dir="rtl"><meta charset="utf-8"><link rel="stylesheet" href="/css/cinema.css"><main class="admin-main" style="margin:0"><section class="admin-card"><h1>{{ $venue->exists?'ویرایش سالن':'ثبت سالن' }}</h1><form method="post" action="{{ $venue->exists?route('admin.venues.update',$venue):route('admin.venues.store') }}">@csrf @if($venue->exists) @method('PUT') @endif<label>نام سالن<input class="input" name="name" value="{{ $venue->name }}" required></label><label>شهر<input class="input" name="city" value="{{ $venue->city?:'تهران' }}" required></label><label>آدرس<input class="input" name="address" value="{{ $venue->address }}"></label><label>تعداد ردیف<input class="input" type="number" name="rows" value="{{ $venue->exists?intdiv($venue->capacity,12):8 }}" min="1" max="30" required></label><label>صندلی در هر ردیف<input class="input" type="number" name="seats_per_row" value="12" min="1" max="50" required></label><p class="muted">در صورت داشتن سانس آینده، نقشهٔ صندلی سالن هنگام ویرایش بازسازی نمی‌شود.</p><button class="primary">{{ $venue->exists?'ذخیره تغییرات':'ذخیره سالن' }}</button></form></section></main>
