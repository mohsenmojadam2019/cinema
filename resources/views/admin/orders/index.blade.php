<!doctype html><html lang="fa" dir="rtl"><meta charset="utf-8"><link rel="stylesheet" href="/css/cinema.css"><main class="admin-main" style="margin:0"><h1>مدیریت سفارش‌ها</h1><section class="admin-card"><table><tr><th>کد</th><th>رویداد</th><th>سالن</th><th>مبلغ</th><th>وضعیت</th><th></th></tr>@foreach($orders as $order)<tr><td>{{ $order->code }}</td><td>{{ $order->show->event->title }}</td><td>{{ $order->show->venue->name }}</td><td>{{ number_format($order->total) }}</td><td>{{ $order->status }}</td><td>@if($order->status==='paid')<form method="post" action="{{ route('admin.orders.refund',$order) }}">@csrf<button class="ghost">استرداد</button></form>@endif</td></tr>@endforeach</table>{{ $orders->links() }}</section></main>
