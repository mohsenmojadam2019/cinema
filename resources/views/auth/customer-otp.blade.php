<!doctype html><html lang="fa" dir="rtl"><meta charset="utf-8"><link rel="stylesheet" href="/css/cinema.css"><main class="success container"><div class="ticket login-box"><h1>ورود مشتری با پیامک</h1>@if(session('success'))<p class="status">{{ session('success') }}</p>@endif<form method="post" action="{{ route('customer.login.send') }}">@csrf<input class="input" name="phone" placeholder="شماره موبایل" value="{{ session('phone') }}" required><button class="primary full">ارسال کد</button></form><hr><form method="post" action="{{ route('customer.login.verify') }}">@csrf<input class="input" name="phone" placeholder="شماره موبایل" value="{{ session('phone') }}" required><input class="input" name="code" placeholder="کد شش رقمی" required><button class="primary full">ورود</button></form></div></main>
